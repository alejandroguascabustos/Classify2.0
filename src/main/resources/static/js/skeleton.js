
    /* ------------------ Skeleton loader ------------------ */
  const skeleton = document.getElementById('skeleton-loader');
  if (skeleton) {
    setTimeout(() => {
      skeleton.style.display = 'none';
    // carga de 1s
    }, 1500); 
  }
